/**
 * Configuración general del sitio, para ajustes que no ameritan tocar
 * componentes directamente.
 */
export const SITE = {
  /**
   * Foto de fondo del banner principal (hero). Puede ser una URL externa
   * o una ruta local en /public, ej: "/images/banner/promo.jpg".
   * Si queda vacía (""), se usa el fondo degradado negro/dorado actual.
   */
  heroImage: "",
};
